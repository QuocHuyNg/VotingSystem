# ============================================================
# BACKEND .env — Express + SQLite + Blockchain
# ============================================================

PORT=5000
JWT_SECRET=voting_system_super_secret_key_2025
JWT_EXPIRES_IN=7d
NODE_ENV=development

# ── Blockchain ──────────────────────────────────────────────
# Sepolia public RPC (không cần API key)
SEPOLIA_URL=https://ethereum-sepolia-rpc.publicnode.com

# Địa chỉ contract sẽ được cập nhật tự động sau khi chạy:
# npx hardhat run scripts/deploy.js --network sepolia
CONTRACT_ADDRESS=0xa5174C901B71dD4b842209A9300D9aC35759ce5E

# ── Etherscan (tuỳ chọn, để hiển thị link explorer) ────────
ETHERSCAN_BASE_URL=https://sepolia.etherscan.io
